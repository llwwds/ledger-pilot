# Ledger Pilot 账本管家

本地优先的个人记账应用。支持导入并合并微信/支付宝账单、手动补录例外流水、用精确规则预填后逐笔人工确认，并在 Web 看板查看有效标签下的月度财务结果。

## v1.2.1 修复

- 修复热力图请求尚未返回、请求失败或跨年切换时被误画成全年空白格的问题。
- 热力图数据按响应年份校验，旧请求不能覆盖当前年份；加载失败和全年无流水会显示明确状态。
- 新增真实非零日色阶的前端回归测试，避免接口有值但视图仍呈空白。

## v1.2.0 新增

- 新增所选年份的“花钱的热力图”和“赚钱的热力图”，按天展示金额强度与交易笔数。
- 两张热力图使用独立色阶和对数分级，不替换或改变原有月度轨迹、分类、渠道和流水区域。
- 新增只读年度热力图 API；年份切换跟随当前选中月份。

## v1.1.0 新增

- 新增“手动记账”弹窗，支持时间、收支方向、金额、交易对方、摘要、分类、支付渠道、流水号和备注。
- 手动流水使用独立来源，不伪装成微信或支付宝；每条记录保留不可变 JSON 快照和审计日志。
- 可填写外部流水号防止重复补录，分类和支付渠道直接保存为人工确认结果。

## v1.0.0 能力

- 导入微信支付 `.xlsx`、支付宝 `.csv` 或包含这些账单的加密 `.zip`，永久保存每次导入原件和不可变归一化记录；解压密码仅在内存中使用。
- 同时导入多份使用不同密码的压缩包时，可按行输入密码，或直接粘贴“平台：密码”格式的密码文本。
- 以 `(来源平台, 交易单号)` 幂等更新唯一总账；重复导入仍保留批次记录并写审计日志。
- 管理任意层级标签树：维度、父子标签、备注、排序、启停和使用次数。
- 安全删除标签：无引用时删除，有流水引用或子标签时自动停用。
- 配置“交易对方完整名称”的精确匹配规则，只负责给人工标注弹窗预填默认值。
- 在流水弹窗中查看原始事实、修改标签、保存、上一条、跳过或保存并下一条。
- 人工结果始终优先于规则建议；看板和流水明确展示“人工 / 规则 / 未确认”来源。
- 查看月度收支、趋势、支出分类、支付渠道和可筛选流水。
- 查询重复导入、导入失败、规则、标签和人工标注审计日志。

v1 不接入任何 AI 服务，不需要模型 URL、API Key 或外部网络。也不提供预算、删除原始流水或桌面壳。

## 数据流

```text
微信 xlsx / 支付宝 csv / 加密 zip
          │
          ▼
  导入批次 + 原件留存
          │
          ▼
   不可变归一化记录
          │
          ├── 重复：保留记录 + 审计
          ▼
      SQLite 唯一总账
          │
          ├── 精确规则建议
          ├── 人工确认（优先）
          ▼
      月度看板与流水
```

运行数据默认保存在 `backend/data/`，已被 Git 忽略；原始账单和数据库不会进入版本库。

## 本地启动

需要 Python 3.11+ 与 Node.js 20+。

### 1. 后端

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

健康检查：`http://127.0.0.1:8000/api/health`；OpenAPI：`http://127.0.0.1:8000/docs`。

### 2. 前端

```bash
cd frontend
npm install
npm run dev
```

浏览器打开 `http://127.0.0.1:5173`。Vite 会把 `/api` 代理到本机 `8000` 端口。

## 使用顺序

1. 在“标签管理”中确认或扩展收入分类、支出分类、支付渠道、业务类型和特殊标签。
2. 在“预填规则”中为高频交易对方创建精确规则。
3. 导入微信或支付宝账单；现金、银行卡直入等例外流水可使用“手动记账”。
4. 在“流水标注”中点击“标注”，核对建议并连续保存。
5. 返回月度看板查看人工确认后的统计。

被引用的标签删除后会转为停用，历史引用不会丢失。规则只匹配交易对方完整名称，不匹配关键词或商品说明。

## 运行参数

- `LEDGER_PILOT_DATA_DIR`：导入原件目录。
- `LEDGER_PILOT_DATABASE_URL`：覆盖默认 SQLite URL。
- `LEDGER_PILOT_CORS_ORIGINS`：允许访问 API 的 Web 来源，逗号分隔。

## 验证

```bash
cd backend
pytest

cd ../frontend
npm run lint
npm run typecheck
npm test -- --run
npm run build
```

## 项目结构

```text
backend/app/annotations.py  标签目录、规则建议、人工优先级与审计
backend/app/importers.py    微信/支付宝解析与事实归一化
backend/app/main.py         FastAPI 与 v1 API
backend/app/models.py       总账、标签树、规则、分配和审计模型
frontend/src/App.tsx        看板、标签管理、规则管理与人工标注弹窗
```
