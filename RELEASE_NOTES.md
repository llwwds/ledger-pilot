# Ledger Pilot 发布说明

本文件维护每个正式版本的发布页标题、版本描述和核心变更，可直接作为 GitHub Release 的说明来源。版本按 SemVer 推进；对应 tag 只应创建在已提交、通过验证且可复现的 commit 上。

## v1.7.0 — 开源发布准备

发布日期：2026-08-26

把本地个人应用整理为可公开复现的源码仓库，同时明确区分“可提交的安全默认值”和“永不进入版本库的用户数据与配置”。

- 加入 MIT 许可证，并补齐开源发布、本地运行、隐私边界和配置优先级说明。
- 加强 Git 忽略规则：账单原件、SQLite 数据库、本地 JSON 配置、环境变量、依赖、缓存和构建物均不进入仓库。
- 新增公开的 `backend/config.default.json` 和被忽略的 `backend/config.local.json`。
- 首次启动只在本地配置整个缺失时创建默认文件；已有文件即使为空也不改写，部分配置缺失项仅在内存中使用公开默认值。
- 配置按显式参数、环境变量、本地 JSON、公开默认值的顺序合并后再校验，并支持 `LEDGER_PILOT_CONFIG_FILE` 指向仓库外的配置文件。
- 新配置以仅当前用户可读写的 `0600` 权限创建；移除数据库模块导入时创建默认数据库的副作用。
- 增加缺失、空文件、部分覆盖、优先级、权限、路径和未知字段的初始化回归测试。

## v1.6.0 — 结构化流水筛选

发布日期：2026-08-26

把“搜一段文字”升级为可组合的结构化筛选器，让一次批量标注可以精确复用流水事实字段，而不必先导出或反复翻页。筛选只影响候选集合，不会改写原始流水或人工标注。

- 高级筛选覆盖来源平台、交易时间、收支方向、金额、交易对方、商品说明、支付方式、支付渠道、交易状态、状态分类、交易单号、商户单号、交易类型、来源分类、对方账号和备注共 16 个归一化字段。
- 同字段包含条件取并集、跨字段取交集，排除条件统一否决；全局模糊搜索可切换包含或排除，并与所有字段条件正交组合。
- 文本字段支持精确、模糊和为空，日期与金额支持包含首尾的范围；纯日期上限覆盖当天全天，具体时间上限保持精确。
- 日期条件会接管并替换月份快捷轨道，避免跨月筛选被隐藏截断，并可一键恢复月份范围；已应用条件以筛选标签展示，可单独移除或一次清空。
- 任一筛选变化都会立即废弃旧请求、进入加载态并清空批量选择，避免 debounce 窗口或慢响应把旧流水重新带回操作区。
- 新增严格校验的 `POST /api/transactions/search`，限制条件数量和查询长度，并保留旧查询接口以兼容现有调用方。
- 增加全部字段、组合逻辑、边界日期、请求序列化、批量选择和过期响应隔离的后端与前端回归测试。

## v1.5.1 — 总看板边界与交互修复

发布日期：2026-08-26

- 限制自定义日期在 1900 至 2100 年，避免极端上界日期导致后端溢出。
- 日期输入和范围颗粒度切换都会按完整周或完整月吸附，与日历点击行为一致。
- 跨年趋势保留年份，自定义热力图固定跟随范围开始年份。
- 窄屏日期选择器改为可横向滚动的 20px 日历格，提高触屏点选可靠性。

## v1.5.0 — 多时间范围总看板

发布日期：2026-08-26

把单一月度复盘扩展成统一的年度、月度和自定义时间范围看板，同时让曲线显示颗粒度成为独立控制。相同的收支汇总、趋势和标签分布会从同一组时间边界计算，避免不同视角之间口径漂移。

- 主导航中的“月度看板”升级为“总看板”，可切换年度、月度和自定义范围。
- 自定义范围支持日期输入和全年日历点选，并可按日、周、月吸附完整周期。
- 收支趋势可独立切换日、周、月数据点，不改变当前统计范围。
- 看板 API 支持年度、自定义首尾闭区间和趋势颗粒度，同时保留原月度查询兼容性。
- 增加范围边界、跨周/跨月聚合、请求参数和前端范围交互回归测试。

## v1.4.1 — 标签管理交互修复

发布日期：2026-08-25

修复标签新增与删除按钮在当前运行容器中看似“没有反应”的问题。版本对比确认 v1.4.0 没有直接改动标签 CRUD；真正原因是旧交互依赖浏览器原生 `prompt()` / `confirm()`，运行容器不支持 `prompt()` 时会在点击后抛错，而异常又发生在原有请求错误处理之外，页面没有可见反馈。

- “新增根标签 / 新增子级 / 新增同级”改为应用内受控创建弹窗。
- 标签和维度的安全删除改为应用内确认弹窗。
- 创建弹窗同时支持标签名称和备注，并按当前父级的同级数量生成排序值。
- 创建、删除期间禁用重复提交；接口失败时在弹窗内保留输入并显示错误。
- 新增真实交互回归测试，避免再次退化为仅检查按钮存在。

## v1.4.0 — 批量标注工作台

发布日期：2026-08-25

把原本藏在月度看板底部的逐笔标注，升级为一个专门处理重复确认工作的独立工作台。现在可以先搜索出一组相似流水，多选后一次保存相同标签；人工确认完成的流水会立即离开队列，让页面始终只呈现真正还需要处理的事项。

- 新增与“月度看板”平行的“流水标注”一级页面。
- 保留商户、商品、备注搜索以及分类、渠道筛选，并增加逐行多选和全选当前结果。
- 新增原子批量标注接口，批内任一流水不兼容时整体回滚，成功时仍逐笔记录审计。
- 待标注队列以人工确认时间为准：规则预填不算完成，人工保存或明确留空后即移出。
- 混合收入与支出时隐藏方向专属分类，只允许统一设置共同维度。
- 修正单笔连续标注在“保存后移除”场景下可能跳过下一笔的导航逻辑。

## v1.3.0 — 双皮肤账本

发布日期：2026-08-24

在不改变账本内容结构的前提下，建立可切换、可持久化的完整视觉皮肤系统，让长期使用时可以在克制的纸面编辑感与更沉浸的深色毛玻璃界面之间切换。

- 将原有浅雾蓝纸面风格命名为“雾蓝账页”。
- 新增深海蓝、靛紫环境光与毛玻璃质感的“流光雾镜”。
- 页头可即时切换，选择仅保存在本机。
- 图表、热力图、表单、弹窗和管理页同步换肤。
- 适配窄屏和减少动态效果偏好。

## v1.2.2 — 自适应维度看板

发布日期：2026-08-24

让月度看板跟随标签体系自然增长，不再要求每增加一个一级维度就修改图表代码，同时改善多图表下的信息密度。

- 日收支轨迹调整为独占整行，避免与分布图横向挤压。
- 所有启用的一级标签维度自动生成饼图卡片。
- 桌面端每行两张分布图，窄屏自动切换为单列。
- 保留左侧图形、右侧金额明细的既有阅读结构。

## v1.2.1 — 热力图可靠性修复

发布日期：2026-08-24

修复年度热力图在加载、失败和跨年切换过程中被误画为空白的问题，使“没有数据”和“数据还没回来”成为两个明确状态。

- 请求未完成或失败时不再渲染伪空白全年格子。
- 按响应年份校验数据，旧请求不能覆盖当前年份。
- 为真实非零日期色阶补充前端回归测试。

## v1.2.0 — 年度资金热力

发布日期：2026-08-24

在月度分析之外增加按日回看的年度视角，用两张方向独立的热力图快速定位花钱和赚钱最集中的日期。

- 新增年度支出热力图和收入热力图。
- 按每日金额与交易笔数展示强度，采用独立色阶和对数分级。
- 新增只读年度热力图 API，年份跟随当前选中月份。
- 不替换原有月度轨迹、分类、渠道和流水内容。

## v1.1.0 — 手动记账

发布日期：2026-08-24

补齐平台账单之外的例外流水入口，让现金、银行卡直入等记录也能进入同一份可审计、可分类的本地账本。

- 新增手动记账弹窗，支持时间、方向、金额、对方、摘要、分类、渠道、流水号和备注。
- 手动流水使用独立来源，不伪装成微信或支付宝数据。
- 保留不可变 JSON 快照和审计日志。
- 可填写外部流水号防重复，分类和渠道直接保存为人工确认结果。

## v1.0.0 — 本地账本与人工标注基线

发布日期：2026-08-24

Ledger Pilot 的首个正式版本：在本地完成微信、支付宝账单的可追溯合并，以确定性规则提供建议，再由用户对标签结果做最终确认；整个主流程不依赖 AI 或外部服务。

- 导入微信 `.xlsx`、支付宝 `.csv` 和包含账单的加密 `.zip`。
- 永久保留导入原件、不可变归一化记录、重复批次和审计证据。
- 以来源平台和交易单号幂等合并唯一总账。
- 提供任意层级标签树、启停、排序、使用次数和安全删除。
- 提供交易对方完整名称精确匹配的规则预填。
- 支持逐笔人工标注、规则建议、人工优先级和连续处理。
- 提供月度收支、趋势、分类、渠道、流水筛选和审计查询。
- 不接入 AI，不需要模型地址、API Key 或外部网络。
